# 模型部署建议

比赛MVP使用本地、可配置、支持结构化输出或工具调用的轻量中文模型。稳定性来自规则优先、固定五项Skill、少量工具、Schema、程序查数、确认和降级，而不是单纯换更大的模型。

## 启动要求
- 系统启动后模型预热；
- 健康检查通过才显示AI已就绪；
- 模型未加载时查询转标准表单；
- 写操作模型异常时阻断；
- 比赛前保持模型常驻；
- 不依赖场地网络。

## 参数建议
- temperature: 0.1
- 单次工具: 2至4个
- 最大候选Skill: 3
- 请求超时: 10秒
- 仅加载命中Skill内容
- 输出必须通过JSON Schema
